__pycache__/
*.pyc
.streamlit/secrets.toml

# local-only eBay Sell (sandbox) secrets -- never commit
.ebay_tokens.json
ebay_config.json
.env
/android/.gradle/
/android/build/
/android/app/build/
/android/app/google-services.json
/android/local.properties

# Release signing (Play upload key): the real keystore and its passwords
# must never be committed. See docs/RELEASE_SIGNING.md.
/android/keystore.properties
*.keystore
!/android/appraze-debug.keystore
*.jks
